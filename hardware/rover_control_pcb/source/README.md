# Native Flux source

The editable master design was exported from Flux as:

`garciamatthew-uhcl-lunar-hawks-rover-control-and-safety-pcb-rev-a.flx`

Original export SHA-256:

`2326fa1ba36ae34d8cafc7df1b7e659d40a6dbe5e93c127aa8d41305cb08daca`

The `.flx` project contains the native Flux schematic/PCB project data. The repository also carries portable schematic/layout exports under `../schematic/flux/` and `../pcb/flux/` so the current electrical design state is represented outside the native Flux format.

The Rev A board is still in development: placement is complete, but bulk routing, final DRC, fabrication and hardware validation are pending.
